<form method="post" action="{{ path('admin_livraison_delete', {'id': livraison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('annuler' ~ livraison.id) }}">
    <button class="btn">Supprimer</button>
</form>
