{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
<div style="display: flex; justify-content: center; align-items: center; height: 100vh;">
    <div>
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>

                <th>{{ panier.produit.nom }}</th>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ panier.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_index') }}"style="background-color: #d1a88e; color: #6e2424;">retour à la liste</a>

    <a href="{{ path('app_panier_edit', {'id': panier.id}) }}"style="background-color: #d1a88e; color: #6e2424;">Modifier</a>

    {{ include('panier/_delete_form.html.twig') }}
    </div>
</div>
        {% endblock %}
