{% extends 'baseback.html.twig' %}

{% block title %}Panier{% endblock %}

{% block content %}
<div style="display: flex; justify-content: center; align-items: center; height: 100vh;">
    <div>
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>

                <th>{{ panier.produit.nom }}</th>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ panier.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_panier_index') }}"style="background-color: #d1a88e; color: #6e2424;">retour à la liste</a>

    <a href="{{ path('admin_panier_edit', {'id': panier.id}) }}"style="background-color: #d1a88e; color: #6e2424;">Modifier</a>

    {{ include('panierAdmin/_deleteadmin_form.html.twig') }}
    </div>
</div>
        {% endblock %}
