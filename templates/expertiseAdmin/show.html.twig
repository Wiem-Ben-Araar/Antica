{% extends 'baseback.html.twig' %}

{% block title %}Expertise{% endblock %}

{% block content %}
    <h1>Expertise</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Id_produit</th>
                <td>{{ expertise.idProduit }}</td>
            </tr>

            <tr>
                <th>Prix_estime</th>
                <td>{{ expertise.prixEstime }}</td>
            </tr>
            <tr>
                <th>Condition_produit</th>
                <td>{{ expertise.conditionProduit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_expertise_index') }}">back to list</a>

    <a href="{{ path('admin_expertise_edit', {'id': expertise.id}) }}">edit</a>

    {{ include('expertiseAdmin/_delete_form.html.twig') }}
{% endblock %}
